import { registerRootComponent } from 'expo';
import App from './App';

// This line does two things:
// 1. It tells Expo that 'App' is the main component of your application.
// 2. It handles the standard AppRegistry.registerComponent call for you.
registerRootComponent(App);
